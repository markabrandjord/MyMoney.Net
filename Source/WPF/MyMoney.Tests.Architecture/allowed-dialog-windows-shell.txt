# D-15 enforcement allow-list, MyMoney.Shell edition: every System.Windows.Window-derived,
# non-abstract type in the new MyMoney.Shell WPF app must be listed here, one fully-qualified
# type name per line. Same contract as allowed-dialog-windows.txt next to it, applied to the
# project where new dialogs will actually be written from now on - a new Window here is a
# deliberate, reviewable line in this file or DialogAllowListTests fails.
#
# Generated 2026-09-21 by reflecting over the actual built MyMoney.Shell.dll
# (typeof(Window).IsAssignableFrom(t) && !t.IsAbstract) via DialogAllowListTests itself - not a
# guess. MainWindow is the only one: it derives from WPF-UI's Wpf.Ui.Controls.FluentWindow,
# which is a Window. Every "dialog" this app shows so far is a ContentDialog hosted inside that
# one window (D-13's DialogService), which is exactly the shape D-15 wants and is why this list
# is one line long.
MyMoney.Shell.MainWindow
