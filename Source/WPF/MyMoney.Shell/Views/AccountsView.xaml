<!-- Source/WPF/MyMoney.Shell/Views/AccountsView.xaml -->
<UserControl x:Class="MyMoney.Shell.Views.AccountsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:resources="clr-namespace:MyMoney.Shell.Resources"
             AutomationProperties.AutomationId="AccountsPage">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <!-- TabIndex 10-13: continues MainWindow.xaml's reading-order sequence (see its own
             comment) - this content is hosted at TabIndex 6's NavAccountsButton and before
             TabIndex 100's ActivityButton, so this screen's own controls get the range between,
             in their own visual top-to-bottom order (buttons row, then the grid). -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,8">
            <ui:Button x:Name="AddAccountButton" AutomationProperties.AutomationId="AddAccountButton"
                       Content="{x:Static resources:Strings.AddAccountButtonLabel}" Appearance="Primary"
                       Margin="0,0,8,0" Click="AddAccountButton_Click" TabIndex="10"/>
            <ui:Button x:Name="EditAccountButton" AutomationProperties.AutomationId="EditAccountButton"
                       Content="{x:Static resources:Strings.EditAccountButtonLabel}" IsEnabled="False"
                       Margin="0,0,8,0" Click="EditAccountButton_Click" TabIndex="11"/>
            <ui:Button x:Name="DeleteAccountButton" AutomationProperties.AutomationId="DeleteAccountButton"
                       Content="{x:Static resources:Strings.DeleteAccountButtonLabel}" IsEnabled="False"
                       Click="DeleteAccountButton_Click" TabIndex="12"/>
        </StackPanel>
        <ListView x:Name="AccountsListView" AutomationProperties.AutomationId="AccountsListView"
                  Grid.Row="1" ItemsSource="{Binding Accounts}"
                  SelectionChanged="AccountsListView_SelectionChanged" TabIndex="13">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="{x:Static resources:Strings.FieldLabelName}" DisplayMemberBinding="{Binding Name}" Width="200"/>
                    <GridViewColumn Header="{x:Static resources:Strings.FieldLabelType}" DisplayMemberBinding="{Binding Type}" Width="140"/>
                    <GridViewColumn Header="{x:Static resources:Strings.FieldLabelCurrency}" DisplayMemberBinding="{Binding Currency}" Width="100"/>
                    <GridViewColumn Header="{x:Static resources:Strings.FieldLabelOpeningBalance}" DisplayMemberBinding="{Binding OpeningBalance, StringFormat=C}" Width="140"/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</UserControl>
