<!-- Source/WPF/MyMoney.Shell/Views/DeleteAccountView.xaml -->
<UserControl x:Class="MyMoney.Shell.Views.DeleteAccountView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:resources="clr-namespace:MyMoney.Shell.Resources"
             Width="320">
    <StackPanel>
        <TextBlock x:Name="ConfirmText" AutomationProperties.AutomationId="DeleteAccountConfirmText"
                   TextWrapping="Wrap" Margin="0,0,0,12">
            <Run Text="{x:Static resources:Strings.DeleteAccountConfirmPrefix}"/>
            <Run Text="{Binding AccountName, Mode=OneWay}" FontWeight="SemiBold"/>
            <Run Text="{x:Static resources:Strings.DeleteAccountConfirmSuffix}"/>
        </TextBlock>

        <TextBlock x:Name="ErrorText" AutomationProperties.AutomationId="DeleteAccountErrorText"
                   Text="{Binding ErrorMessage}"
                   Foreground="{DynamicResource SystemFillColorCriticalBrush}"
                   Visibility="{Binding ErrorMessage, Converter={StaticResource NullToCollapsedConverter}}"/>
    </StackPanel>
</UserControl>
