using System;

namespace Walkabout.Data
{
    /// <summary>
    /// Placeholder entry point for the MyMoneyAdmin console tool.
    /// The real CLI behavior (sa bootstrap connection, SQL Server admin
    /// operations, etc.) is added in later tasks of the SQL Server
    /// dual-engine plan.
    /// </summary>
    internal class Program
    {
        private static int Main(string[] args)
        {
            Console.WriteLine("MyMoneyAdmin");
            return 0;
        }
    }
}
